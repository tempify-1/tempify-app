import { component$ } from '@builder.io/qwik';
import type { AppFormFieldProps } from '../app-form/form-types';

export const AppFormLocation = component$<AppFormFieldProps>(() => {
  return <div>AppFormLocation component works!</div>;
});
