import { component$ } from '@builder.io/qwik';
import type { MfFieldProps } from '../mf-form/form-types';

export const MfLocation = component$<MfFieldProps>(() => {
  return <div>MfLocation component works!</div>;
});
